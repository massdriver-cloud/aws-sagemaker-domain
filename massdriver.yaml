schema: draft-07
name: "aws-sagemaker-domain"
description: "AWS SageMaker Domain and User Profile for SageMaker Studio AI Research Platform"
source_url: github.com/YOUR_NAME_HERE/aws-sagemaker-domain
access: private
type: "infrastructure"

# schema-params.json
# JSON Schema sans-fields above
params:
  properties: {}

connections:
  required:
  - aws_authentication
  - s3_model_bucket
  - vpc
  properties:
    aws_authentication:
      $ref: massdriver/aws-iam-role
    s3_model_bucket:
      $ref: massdriver/aws-s3-bucket
    vpc:
      $ref: massdriver/aws-vpc

artifacts:
  properties: {}

ui:
  ui:order:
    - user_profile_name
    - "*"